(()=>{
  if(window.__ProvedorPlusDashboardTransitionGuardInstalled)return;
  window.__ProvedorPlusDashboardTransitionGuardInstalled=true;

  const normalize=value=>String(value??'').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\s+/g,' ');
  let dashboardIntent=false,transitionObserver=null,stopTimer=null;

  function dashboardButton(target){
    const button=target?.closest?.('nav button,.sidebar nav button');
    if(!button)return null;
    const text=normalize(button.textContent);
    return {button,isDashboard:text.includes('dashboard')||text.includes('visao geral')};
  }

  function activeDashboard(){
    const active=document.querySelector('nav button.active,.sidebar nav button.active');
    if(!active)return false;
    const text=normalize(active.textContent);
    return text.includes('dashboard')||text.includes('visao geral');
  }

  function protectedHosts(){return [...document.querySelectorAll('.page-wrap.pp-dashboard-host')]}

  function protectCurrentHost(){
    if(!dashboardIntent)return;
    const host=document.querySelector('.page-wrap');
    if(host&&!host.classList.contains('pp-dashboard-host'))host.classList.add('pp-dashboard-host');
  }

  function releaseDashboardHosts(){for(const host of protectedHosts())host.classList.remove('pp-dashboard-host')}

  function stopTransitionObserver(){
    if(transitionObserver){transitionObserver.disconnect();transitionObserver=null}
    clearTimeout(stopTimer);stopTimer=null;
  }

  function beginDashboardTransition(){
    dashboardIntent=true;
    protectCurrentHost();
    stopTransitionObserver();
    transitionObserver=new MutationObserver(()=>{
      if(!dashboardIntent){stopTransitionObserver();return}
      protectCurrentHost();
      if(document.querySelector('.page-wrap.pp-dashboard-host > .pp-dashboard-v2')){
        requestAnimationFrame(()=>{if(dashboardIntent)protectCurrentHost();stopTransitionObserver()});
      }
    });
    transitionObserver.observe(document.documentElement,{childList:true,subtree:true});
    queueMicrotask(protectCurrentHost);
    requestAnimationFrame(protectCurrentHost);
    stopTimer=setTimeout(stopTransitionObserver,1000);
  }

  function leaveDashboardTransition(){
    dashboardIntent=false;
    stopTransitionObserver();
    const settle=()=>{
      if(activeDashboard())return;
      releaseDashboardHosts();
      stopTransitionObserver();
    };
    const nav=document.querySelector('nav,.sidebar nav');
    if(nav){
      transitionObserver=new MutationObserver(settle);
      transitionObserver.observe(nav,{attributes:true,subtree:true,attributeFilter:['class'],childList:true});
    }
    requestAnimationFrame(()=>requestAnimationFrame(settle));
    stopTimer=setTimeout(settle,700);
  }

  function onNavigationIntent(event){
    const nav=dashboardButton(event.target);if(!nav)return;
    if(nav.isDashboard)beginDashboardTransition();
    else leaveDashboardTransition();
  }

  document.addEventListener('click',onNavigationIntent,true);
})();

(()=>{
  if(window.__ProvedorPlusIntegrationHubInstalled)return;
  window.__ProvedorPlusIntegrationHubInstalled=true;
  const norm=v=>String(v??'').trim().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\s+/g,' ');
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const MASK='••••••••••';
  let hubButton=null,popup=null,layer=null,timer=null,navObserver=null;
  const navRoot=()=>document.querySelector('aside.sidebar nav[aria-label="Menu principal"],aside.sidebar nav');
  const contentRoot=()=>document.querySelector('.app-shell>.content')||document.querySelector('.content');
  function styles(){if(document.getElementById('pp-integration-hub-style'))return;const e=document.createElement('style');e.id='pp-integration-hub-style';e.textContent=`
    .pp-integration-hub-button{position:relative!important}.pp-integration-chevron{margin-left:auto!important;opacity:.62}.pp-integration-menu{position:fixed;z-index:11050;width:230px;padding:7px;background:#fff;border:1px solid #dce7e4;border-radius:11px;box-shadow:0 14px 38px rgba(18,52,45,.18)}.pp-integration-menu[hidden]{display:none!important}.pp-integration-menu button{display:flex;width:100%;align-items:center;gap:10px;padding:10px 11px;border:0;border-radius:8px;background:transparent;color:#294b44;text-align:left;font:700 11px Segoe UI,Arial;cursor:pointer}.pp-integration-menu button:hover{background:#edf8f5;color:#087866}.content.pp-integration-hub-active>:not(.pp-integration-hub-layer){display:none!important}.content.pp-integration-hub-active>.pp-integration-hub-layer{display:block!important}.pp-integration-hub-layer{display:none;min-height:100%;padding:28px;background:#f4f7f6;box-sizing:border-box;font-family:Segoe UI,Arial;color:#173d36}.pp-bank-head{display:flex;justify-content:space-between;gap:20px;align-items:flex-start;margin-bottom:16px}.pp-bank-head h1{margin:4px 0 7px;font-size:27px}.pp-bank-head p{margin:0;color:#6e807b;font-size:12px}.pp-bank-kicker{font-size:10px;font-weight:900;letter-spacing:.12em;color:#07806e}.pp-bank-default{display:flex;align-items:center;gap:8px;font-size:10px;font-weight:800;color:#5c706b}.pp-bank-default select,.pp-bank-fields input,.pp-bank-fields select{height:38px;border:1px solid #cfe0dc;border-radius:9px;background:#fff;padding:0 10px;color:#294b44;box-sizing:border-box}.pp-bank-summary{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:11px;margin-bottom:14px}.pp-bank-summary article,.pp-bank-card{background:#fff;border:1px solid #dce7e4;border-radius:13px;box-shadow:0 4px 16px rgba(30,72,63,.04)}.pp-bank-summary article{padding:14px}.pp-bank-summary small{display:block;font-size:9px;font-weight:850;color:#788984}.pp-bank-summary strong{display:block;margin-top:5px;font-size:13px}.pp-bank-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.pp-bank-card{padding:18px}.pp-bank-card-head{display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:14px}.pp-bank-title{display:flex;align-items:center;gap:10px}.pp-bank-logo{display:grid;place-items:center;width:36px;height:36px;border-radius:10px;background:#e9f8f2;color:#087866;font-weight:900}.pp-bank-logo.mp{background:#edf5ff;color:#176db5}.pp-bank-title strong{display:block;font-size:14px}.pp-bank-title small{display:block;margin-top:2px;color:#7a8a86;font-size:9px}.pp-bank-status{padding:5px 8px;border-radius:999px;background:#f0f4f3;color:#6b7b77;font-size:9px;font-weight:850}.pp-bank-status.ok{background:#e8f8f1;color:#08735d}.pp-bank-status.error{background:#fff0ed;color:#ae4935}.pp-bank-fields{display:grid;grid-template-columns:1fr 1fr;gap:9px}.pp-bank-fields label{display:grid;gap:5px;font-size:10px;font-weight:750;color:#4d625d}.pp-bank-fields .full{grid-column:1/-1}.pp-bank-switch{display:flex!important;align-items:center;justify-content:space-between;grid-column:1/-1;padding:9px 10px;border:1px solid #e0e9e7;border-radius:9px;background:#fafcfb}.pp-bank-switch input{width:17px;height:17px}.pp-bank-note{margin:10px 0 0;color:#778884;font-size:9px;line-height:1.45}.pp-bank-cert{grid-column:1/-1;font-size:9px;color:#6d7e79}.pp-bank-cert.ok{color:#087866;font-weight:850}.pp-bank-actions{display:flex;flex-wrap:wrap;gap:8px;margin-top:14px}.pp-bank-actions button{height:36px;padding:0 12px;border:1px solid #cfe0dc;border-radius:9px;background:#fff;color:#31534c;font-size:10px;font-weight:850;cursor:pointer}.pp-bank-actions .primary{border-color:#087866;background:#087866;color:#fff}.pp-bank-message{margin-bottom:14px;padding:11px 13px;border-radius:9px;background:#eaf7f3;color:#126c5a;font-size:10px}.pp-bank-message.error{background:#fff0ed;color:#a64130}.pp-bank-loading{padding:18px;background:#fff;border:1px solid #dce7e4;border-radius:12px}.pp-router-grid{display:grid;grid-template-columns:minmax(310px,.85fr) minmax(420px,1.15fr);gap:14px}.pp-router-list{display:grid;gap:10px}.pp-router-item{padding:12px;border:1px solid #e0e9e7;border-radius:11px;background:#fbfdfc}.pp-router-main{display:flex;align-items:flex-start;justify-content:space-between;gap:12px}.pp-router-main strong{display:block;font-size:12px;color:#1b463e}.pp-router-main small{display:block;margin-top:4px;color:#748681;font-size:9px}.pp-router-actions{display:flex;flex-wrap:wrap;gap:6px;margin-top:10px}.pp-router-actions button{height:31px;padding:0 9px;border:1px solid #d5e3df;border-radius:8px;background:#fff;color:#49635d;font-size:9px;font-weight:800;cursor:pointer}.pp-router-actions button.primary{background:#087866;border-color:#087866;color:#fff}.pp-router-actions button.danger{color:#ad493e;border-color:#edcec9;background:#fffafa}.pp-router-empty{padding:16px;border:1px dashed #cfdfdb;border-radius:10px;color:#70827d;font-size:10px;line-height:1.5}.pp-router-output{margin-top:12px;padding:12px;border:1px solid #dce7e4;border-radius:10px;background:#f8fbfa}.pp-router-output strong{font-size:10px}.pp-router-output pre{max-height:250px;overflow:auto;margin:9px 0 0;padding:10px;border-radius:8px;background:#102823;color:#d9eee8;font:10px/1.45 Consolas,monospace;white-space:pre-wrap}.pp-bank-secure{display:flex;align-items:center;gap:6px;margin-top:10px;color:#61736e;font-size:9px}@media(max-width:980px){.pp-router-grid,.pp-bank-grid,.pp-bank-summary{grid-template-columns:1fr}.pp-bank-fields{grid-template-columns:1fr}.pp-bank-fields .full,.pp-bank-switch,.pp-bank-cert{grid-column:1}.pp-bank-head{display:block}.pp-bank-default{margin-top:12px}.pp-integration-hub-layer{padding:18px 12px}}
  `;document.head.appendChild(e)}
  async function bankCall(action,data={}){const r=await fetch('/api/bank-settings',{method:'POST',headers:{'Content-Type':'application/json'},credentials:'same-origin',cache:'no-store',body:JSON.stringify({action,data})});let b={};try{b=await r.json()}catch{}if(!r.ok||!b.ok)throw new Error(b.error||`Falha bancária (${r.status})`);return b.data}
  function closePopup(){clearTimeout(timer);timer=null;if(popup)popup.hidden=true;hubButton?.setAttribute('aria-expanded','false')}
  function position(){if(!popup||popup.hidden||!hubButton)return;const r=hubButton.getBoundingClientRect(),w=popup.offsetWidth||230;let left=r.right+8;if(left+w>innerWidth-10)left=Math.max(10,r.left-w-8);popup.style.left=`${Math.round(left)}px`;popup.style.top=`${Math.max(10,Math.min(r.top,innerHeight-(popup.offsetHeight||110)-10))}px`}
  function showPopup(){ensureButton();if(!popup||!hubButton)return;clearTimeout(timer);popup.hidden=false;hubButton.setAttribute('aria-expanded','true');requestAnimationFrame(position)}
  function buildPopup(){if(popup?.isConnected)return;popup=document.createElement('div');popup.className='pp-integration-menu';popup.hidden=true;popup.innerHTML='<button type="button" data-kind="banks"><span>▰</span><span>API Bancos</span></button><button type="button" data-kind="mikrotik"><span>▣</span><span>Servidor MikroTik</span></button>';popup.onmouseenter=()=>clearTimeout(timer);popup.onmouseleave=()=>timer=setTimeout(closePopup,150);popup.onclick=e=>{const b=e.target.closest('[data-kind]');if(!b)return;e.preventDefault();e.stopPropagation();openSection(b.dataset.kind)};document.body.appendChild(popup)}
  function openHost(){closePopup();const c=contentRoot();if(!c)return null;c.classList.add('pp-integration-hub-active');layer=c.querySelector(':scope>.pp-integration-hub-layer');if(!layer){layer=document.createElement('section');layer.className='pp-integration-hub-layer';c.appendChild(layer)}navRoot()?.querySelectorAll('button.active').forEach(b=>b.classList.remove('active'));hubButton?.classList.add('active');return layer}
  function statusClass(v){return v==='success'?'ok':v==='error'?'error':''}function statusText(v){return v?.lastTestStatus==='success'?'Conectado':v?.lastTestStatus==='error'?'Falha no teste':v?.enabled?'Configurado':'Desativado'}
  function fileBase64(file){return new Promise((ok,fail)=>{if(!file||!file.size)return ok('');const r=new FileReader;r.onload=()=>ok(String(r.result||'').split(',').pop()||'');r.onerror=()=>fail(new Error('Não foi possível ler o certificado.'));r.readAsDataURL(file)})}
  async function renderBanks(message='',isError=false){const h=openHost();if(!h)return;h.innerHTML='<div class="pp-bank-loading">Carregando API Bancos...</div>';try{const cfg=await bankCall('get-safe'),efi=cfg.efi||{},mp=cfg.mercadoPago||{};let selected='';try{selected=String((await window.provedor?.banks?.get?.())?.defaultProvider||'')}catch{}h.innerHTML=`<div class="pp-bank-head"><div><div class="pp-bank-kicker">INTEGRAÇÃO · API BANCOS</div><h1>Mercado Pago e Efí Bank</h1><p>Configure uma vez. Financeiro e Área do Cliente usam as mesmas faturas e a mesma baixa de pagamento.</p></div><label class="pp-bank-default">Banco padrão<select id="pp-bank-default"><option value="">Automático</option><option value="efi">Efí Bank</option><option value="mercadoPago">Mercado Pago</option></select></label></div>${message?`<div class="pp-bank-message ${isError?'error':''}">${esc(message)}</div>`:''}<div class="pp-bank-summary"><article><small>PIX NA ÁREA DO CLIENTE</small><strong>${efi.enabled&&efi.certificateConfigured&&efi.pixKey?'Efí pronto':mp.enabled&&mp.accessTokenConfigured?'Mercado Pago pronto':'Aguardando banco'}</strong></article><article><small>CARTÃO</small><strong>${mp.enabled&&mp.publicKey&&mp.accessTokenConfigured?'Mercado Pago pronto':'Aguardando Mercado Pago'}</strong></article><article><small>PROTEÇÃO</small><strong>Segredos criptografados</strong></article></div><div class="pp-bank-grid"><article class="pp-bank-card"><div class="pp-bank-card-head"><div class="pp-bank-title"><span class="pp-bank-logo">E</span><span><strong>Efí Bank</strong><small>Boleto, Pix, carnê e Pix Automático</small></span></div><span class="pp-bank-status ${statusClass(efi.lastTestStatus)}">${statusText(efi)}</span></div><form id="pp-efi"><div class="pp-bank-fields"><label class="pp-bank-switch"><span>Ativar Efí Bank</span><input name="enabled" type="checkbox" ${efi.enabled?'checked':''}></label><label>Ambiente<select name="environment"><option value="sandbox">Homologação</option><option value="production">Produção</option></select></label><label>Chave Pix<input name="pixKey" value="${esc(efi.pixKey||'')}" placeholder="Chave cadastrada na Efí"></label><label>Client ID<input name="clientId" value="${efi.clientIdConfigured?MASK:''}" data-saved="${efi.clientIdConfigured?'1':'0'}" placeholder="Client ID"></label><label>Client Secret<input name="clientSecret" type="password" value="${efi.clientSecretConfigured?MASK:''}" data-saved="${efi.clientSecretConfigured?'1':'0'}" placeholder="Client Secret"></label><label>${efi.certificateConfigured?'Substituir certificado P12/PFX':'Certificado P12/PFX'}<input name="certificate" type="file" accept=".p12,.pfx,application/x-pkcs12"></label><label>Senha do certificado<input name="certificatePassword" type="password" value="${efi.certificatePasswordConfigured?MASK:''}" data-saved="${efi.certificatePasswordConfigured?'1':'0'}" placeholder="Se houver"></label><div class="pp-bank-cert ${efi.certificateConfigured?'ok':''}">${efi.certificateConfigured?`✓ Certificado atual salvo e protegido${efi.certificateName?` · ${esc(efi.certificateName)}`:''}`:'Nenhum certificado salvo. Selecione o arquivo P12/PFX.'}</div><label class="full">URL pública do webhook<input name="webhookUrl" value="${esc(efi.webhookUrl||'')}" placeholder="https://.../webhook/efi"></label><label>Agência · Pix Automático<input name="agency" value="${esc(efi.pixAutoReceiverAgency||'')}"></label><label>Conta · Pix Automático<input name="account" value="${esc(efi.pixAutoReceiverAccount||'')}"></label></div><div class="pp-bank-secure">◆ A Área do Cliente nunca recebe Client Secret nem certificado.</div><div class="pp-bank-actions"><button class="primary" type="submit">Salvar e testar</button><button name="save" type="button">Somente salvar</button><button name="webhook" type="button">Configurar webhook</button></div></form></article><article class="pp-bank-card"><div class="pp-bank-card-head"><div class="pp-bank-title"><span class="pp-bank-logo mp">MP</span><span><strong>Mercado Pago</strong><small>Pix e cartão na Área do Cliente</small></span></div><span class="pp-bank-status ${statusClass(mp.lastTestStatus)}">${statusText(mp)}</span></div><form id="pp-mp"><div class="pp-bank-fields"><label class="pp-bank-switch"><span>Ativar Mercado Pago</span><input name="enabled" type="checkbox" ${mp.enabled?'checked':''}></label><label>Ambiente<select name="environment"><option value="sandbox">Teste</option><option value="production">Produção</option></select></label><label>Public Key<input name="publicKey" value="${esc(mp.publicKey||'')}" placeholder="APP_USR-..."></label><label class="full">Access Token<input name="accessToken" type="password" value="${mp.accessTokenConfigured?MASK:''}" data-saved="${mp.accessTokenConfigured?'1':'0'}" placeholder="APP_USR-..."></label></div><div class="pp-bank-secure">◆ Access Token fica somente no servidor; o cliente recebe apenas a Public Key.</div><div class="pp-bank-actions"><button class="primary" type="submit">Salvar e testar</button><button name="save" type="button">Somente salvar</button></div></form></article></div>`;
    const def=h.querySelector('#pp-bank-default');def.value=['efi','mercadoPago'].includes(selected)?selected:'';def.onchange=async()=>{try{await bankCall('save-default',{provider:def.value});await renderBanks('Banco padrão atualizado.')}catch(e){await renderBanks(e.message,true)}};
    h.querySelectorAll('input[data-saved=\"1\"]').forEach(input=>{input.title='Credencial salva com segurança';input.addEventListener('focus',()=>{if(input.value===MASK){input.value='';input.dataset.saved='0'}})});
    const savedValue=value=>String(value||'')===MASK?'':String(value||'');
    const ef=h.querySelector('#pp-efi');ef.elements.environment.value=efi.environment||'sandbox';const saveEfi=async(test)=>{const d=new FormData(ef),file=d.get('certificate'),payload={enabled:d.get('enabled')==='on',environment:String(d.get('environment')),clientId:savedValue(d.get('clientId')),clientSecret:savedValue(d.get('clientSecret')),certificatePassword:savedValue(d.get('certificatePassword')),pixKey:String(d.get('pixKey')||''),pixAutoReceiverAgency:String(d.get('agency')||''),pixAutoReceiverAccount:String(d.get('account')||''),webhookUrl:String(d.get('webhookUrl')||'')};if(file&&file.size){payload.certificateBase64=await fileBase64(file);payload.certificateName=file.name}await bankCall('save-efi',payload);if(test)await bankCall('test-efi');await renderBanks(test?'Efí salva e conexão testada.':'Configuração Efí salva.')};ef.onsubmit=e=>{e.preventDefault();saveEfi(true).catch(x=>renderBanks(x.message,true))};ef.elements.save.onclick=()=>saveEfi(false).catch(x=>renderBanks(x.message,true));ef.elements.webhook.onclick=async()=>{try{await saveEfi(false);await bankCall('configure-efi-webhooks');await renderBanks('Webhook Efí configurado.')}catch(x){await renderBanks(x.message,true)}};
    const mf=h.querySelector('#pp-mp');mf.elements.environment.value=mp.environment||'sandbox';const saveMp=async(test)=>{const d=new FormData(mf);await bankCall('save-mercado-pago',{enabled:d.get('enabled')==='on',environment:String(d.get('environment')),publicKey:String(d.get('publicKey')||''),accessToken:savedValue(d.get('accessToken'))});if(test)await bankCall('test-mercado-pago');await renderBanks(test?'Mercado Pago salvo e conexão testada.':'Configuração Mercado Pago salva.')};mf.onsubmit=e=>{e.preventDefault();saveMp(true).catch(x=>renderBanks(x.message,true))};mf.elements.save.onclick=()=>saveMp(false).catch(x=>renderBanks(x.message,true));
  }catch(e){h.innerHTML=`<div class="pp-bank-message error">${esc(e.message||e)}</div>`}}
  async function renderMikrotik(message='',isError=false){
    const h=openHost();if(!h)return;
    h.innerHTML='<div class="pp-bank-loading">Carregando servidores MikroTik...</div>';
    const api=window.provedor;
    if(!api?.routers?.list||!api?.routers?.save||!api?.mikrotik?.sync){h.innerHTML='<div class="pp-bank-message error">A API MikroTik ainda não foi carregada. Atualize a página e tente novamente.</div>';return}
    try{
      const routers=await api.routers.list(),list=Array.isArray(routers)?routers:[];
      const protectedCount=list.filter(r=>r.has_password).length;
      h.innerHTML=`<div class="pp-bank-head"><div><div class="pp-bank-kicker">INTEGRAÇÃO · SERVIDOR MIKROTIK</div><h1>API MikroTik · RouterOS 7</h1><p>Conexão real via REST HTTPS. O mesmo servidor é usado em clientes, PPPoE, bloqueio, perfis e tráfego.</p></div><span class="pp-bank-status ${list.length?'ok':''}">${list.length?`${list.length} cadastrado${list.length>1?'s':''}`:'Aguardando servidor'}</span></div>${message?`<div class="pp-bank-message ${isError?'error':''}">${esc(message)}</div>`:''}<div class="pp-bank-summary"><article><small>COMUNICAÇÃO</small><strong>RouterOS REST · HTTPS</strong></article><article><small>CREDENCIAIS</small><strong>${protectedCount?`${protectedCount} protegida${protectedCount>1?'s':''}`:'Aguardando senha'}</strong></article><article><small>INTEGRAÇÃO</small><strong>Clientes + PPPoE + tráfego</strong></article></div><div class="pp-router-grid"><article class="pp-bank-card"><div class="pp-bank-card-head"><div class="pp-bank-title"><span class="pp-bank-logo">MT</span><span><strong id="pp-mt-form-title">Adicionar servidor</strong><small>API REST segura para RouterOS 7</small></span></div><span class="pp-bank-status">HTTPS</span></div><form id="pp-mt-form"><input type="hidden" name="id"><div class="pp-bank-fields"><label class="full">Nome do servidor<input name="name" placeholder="Ex.: MikroTik Principal" required></label><label class="full">DNS público ou IP público<input name="host" placeholder="Ex.: mk.seudominio.com.br ou 200.0.0.10" required></label><label>Porta HTTPS<input name="port" type="number" min="1" max="65535" value="443" required></label><label>Usuário RouterOS<input name="username" autocomplete="username" placeholder="Usuário da API" required></label><label class="full">Senha<input name="password" type="password" autocomplete="new-password" placeholder="Informe a senha"></label><label class="pp-bank-switch"><span>Aceitar certificado HTTPS autoassinado</span><input name="allow_self_signed" type="checkbox"></label></div><p class="pp-bank-note">A senha fica protegida no servidor e não é devolvida ao navegador. Ao editar um servidor, deixe a senha vazia para manter a atual.</p><div class="pp-bank-actions"><button class="primary" type="submit">Salvar e conectar</button><button type="button" name="new">Novo</button></div></form></article><article class="pp-bank-card"><div class="pp-bank-card-head"><div class="pp-bank-title"><span class="pp-bank-logo">↔</span><span><strong>Servidores cadastrados</strong><small>Teste real e ferramentas de rede</small></span></div></div><div class="pp-router-list">${list.length?list.map(r=>`<div class="pp-router-item" data-router-id="${Number(r.id)||0}"><div class="pp-router-main"><div><strong>${esc(r.name||'MikroTik')}</strong><small>${esc(r.host||'Sem endereço')}:${Number(r.port)||443} · ${esc(r.username||'Sem usuário')}</small></div><span class="pp-bank-status ${r.has_password?'':'error'}" data-router-live-status="${Number(r.id)||0}">${r.has_password?'Verificando...':'Offline'}</span></div><div class="pp-router-actions"><button type="button" data-action="edit">Editar</button><button type="button" class="primary" data-action="test">Testar agora</button><button type="button" data-action="profiles">Perfis PPPoE</button><button type="button" data-action="remote">Acesso remoto</button><button type="button" class="danger" data-action="delete">Excluir</button></div></div>`).join(''):'<div class="pp-router-empty">Nenhum MikroTik cadastrado. Preencha os dados ao lado e clique em <b>Salvar e conectar</b>.</div>'}</div><div id="pp-mt-output" class="pp-router-output" hidden></div></article></div>`;
      const refreshRouterStatus=async r=>{const badge=h.querySelector(`[data-router-live-status="${Number(r.id)||0}"]`);if(!badge)return;if(!r.has_password){badge.classList.remove('ok');badge.classList.add('error');badge.textContent='Offline';badge.title='Credencial do MikroTik não disponível.';return}try{const live=await api.mikrotik.sync(Number(r.id));if(!badge.isConnected)return;badge.classList.remove('error');badge.classList.add('ok');badge.textContent='Conectado · Online';const identity=live?.identity||live?.routerName||r.name||'MikroTik',version=live?.version||live?.routerVersion||'';badge.title=[identity,version?`RouterOS ${version}`:''].filter(Boolean).join(' · ')}catch(error){if(!badge.isConnected)return;badge.classList.remove('ok');badge.classList.add('error');badge.textContent='Offline';badge.title=error?.message||String(error)}};
      list.forEach(r=>{refreshRouterStatus(r)});
      const form=h.querySelector('#pp-mt-form'),title=h.querySelector('#pp-mt-form-title'),output=h.querySelector('#pp-mt-output');
      const reset=()=>{form.reset();form.elements.id.value='';form.elements.port.value='443';title.textContent='Adicionar servidor';form.elements.name.focus()};
      form.elements.new.onclick=reset;
      form.onsubmit=async e=>{e.preventDefault();const d=new FormData(form),id=Number(d.get('id'))||0,payload={id:id||undefined,name:String(d.get('name')||'').trim(),host:String(d.get('host')||'').trim(),port:Number(d.get('port'))||443,username:String(d.get('username')||'').trim(),password:String(d.get('password')||''),allow_self_signed:d.get('allow_self_signed')==='on'};const submit=form.querySelector('[type="submit"]');submit.disabled=true;submit.textContent='Conectando...';try{const result=await api.routers.save(payload),router=result?.router||result||{},snap=result?.snapshot||{};const detail=[snap.identity||snap.name||'',snap.version?`RouterOS ${snap.version}`:''].filter(Boolean).join(' · ');await renderMikrotik(`Servidor ${router.name||payload.name} salvo e conectado${detail?` · ${detail}`:''}.`)}catch(x){await renderMikrotik(x?.message||String(x),true)}};
      h.querySelector('.pp-router-list')?.addEventListener('click',async e=>{const btn=e.target.closest('button[data-action]');if(!btn)return;const row=btn.closest('[data-router-id]'),id=Number(row?.dataset.routerId)||0,r=list.find(x=>Number(x.id)===id);if(!id||!r)return;const action=btn.dataset.action;
        if(action==='edit'){form.elements.id.value=String(id);form.elements.name.value=r.name||'';form.elements.host.value=r.host||'';form.elements.port.value=String(Number(r.port)||443);form.elements.username.value=r.username||'';form.elements.password.value='';form.elements.allow_self_signed.checked=Boolean(r.allow_self_signed);title.textContent=`Editar · ${r.name||'MikroTik'}`;form.scrollIntoView({behavior:'smooth',block:'start'});return}
        if(action==='delete'){if(!confirm(`Excluir o servidor ${r.name||'MikroTik'}?`))return;try{await api.routers.delete(id);await renderMikrotik('Servidor MikroTik excluído.')}catch(x){await renderMikrotik(x?.message||String(x),true)}return}
        btn.disabled=true;const original=btn.textContent;btn.textContent='Consultando...';try{
          if(action==='test'){const data=await api.mikrotik.sync(id);const identity=data?.identity||data?.routerName||r.name||'MikroTik',version=data?.version||data?.routerVersion||'';await renderMikrotik(`Conexão confirmada com ${identity}${version?` · RouterOS ${version}`:''}.`);return}
          const data=action==='profiles'?await api.mikrotik.profiles(id):await api.mikrotik.remoteAccess(id);output.hidden=false;output.innerHTML=`<strong>${action==='profiles'?'Perfis PPPoE':'Acesso remoto'} · ${esc(r.name||'MikroTik')}</strong><pre>${esc(JSON.stringify(data,null,2))}</pre>`;output.scrollIntoView({behavior:'smooth',block:'nearest'})
        }catch(x){await renderMikrotik(x?.message||String(x),true)}finally{if(btn.isConnected){btn.disabled=false;btn.textContent=original}}
      });
    }catch(e){h.innerHTML=`<div class="pp-bank-message error">${esc(e?.message||e)}</div>`}
  }
  function openSection(kind){closePopup();kind==='banks'?renderBanks():renderMikrotik()}
  function ensureButton(){styles();const nav=navRoot();if(!nav)return;let b=nav.querySelector('[data-pp-integration-hub="1"]'),buttons=[...nav.querySelectorAll('button')],chamados=buttons.find(x=>norm(x.textContent).startsWith('chamados'));if(!b){b=document.createElement('button');b.type='button';b.dataset.ppIntegrationHub='1';b.className=String(chamados?.className||'');b.classList.remove('active');b.classList.add('pp-integration-hub-button');b.innerHTML='<span>⌁</span>Integração<span class="pp-integration-chevron">›</span>';b.setAttribute('aria-haspopup','menu');b.setAttribute('aria-expanded','false');b.onmouseenter=showPopup;b.onmouseleave=()=>timer=setTimeout(closePopup,150);b.onclick=e=>{e.preventDefault();e.stopPropagation();popup&&!popup.hidden?closePopup():showPopup()};nav.insertBefore(b,chamados||null)}else if(chamados&&b.nextSibling!==chamados)nav.insertBefore(b,chamados);hubButton=b;buildPopup();if(!navObserver){navObserver=new MutationObserver(()=>ensureButton());navObserver.observe(nav,{childList:true})}}
  function closeLayer(){contentRoot()?.classList.remove('pp-integration-hub-active');layer?.remove();layer=null;hubButton?.classList.remove('active')}
  document.addEventListener('click',e=>{const nav=navRoot(),b=e.target.closest?.('button');if(b&&nav?.contains(b)&&b!==hubButton){closePopup();closeLayer();return}if(popup&&!popup.hidden&&!e.target.closest('.pp-integration-menu')&&!hubButton?.contains(e.target))closePopup()},true);addEventListener('resize',position);addEventListener('scroll',position,true);
  const rootObserver=new MutationObserver(()=>{
    ensureButton();
    if(navRoot()&&hubButton?.isConnected)rootObserver.disconnect();
  });
  rootObserver.observe(document.documentElement,{childList:true,subtree:true});
  styles();ensureButton();
})();
