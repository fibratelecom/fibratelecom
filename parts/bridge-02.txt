Message=r.message||'Conectado.';save(s);return r}catch(e){s.banks.efi.lastTestAt=now();s.banks.efi.lastTestStatus='error';s.banks.efi.lastTestMessage=e.message;save(s);throw e}}
  async function webhooksEfiWeb(){const s=load(),secrets=await bankSecrets();try{const r=await bankCall({action:'efi-webhooks',efi:secrets.efi});s.banks.efi.webhookConfiguredAt=r.configuredAt||now();s.banks.efi.webhookStatus='success';save(s);return r}catch(e){s.banks.efi.webhookStatus='error';save(s);throw e}}
  async function createPixAutoWeb(values){const s=load(),client=byId(s.clients,values.clientId);if(!client)throw Error('Cliente não encontrado para Pix Automático.');const secrets=await bankSecrets(),r=await bankCall({action:'efi-pix-auto-create',client:m(client),efi:secrets.efi,startDate:values.startDate,endDate:values.endDate||'',amountCents:values.amountCents||0});s.banks.efi.pixAutomaticRecords=[...(s.banks.efi.pixAutomaticRecords||[]).filter(x=>Number(x.clientId)!==Number(client.id)),r].slice(-100);save(s);return r}
  async function refreshPixAutoWeb(clientId){const s=load(),record=(s.banks.efi.pixAutomaticRecords||[]).find(x=>Number(x.clientId)===Number(clientId));if(!record)throw Error('Este cliente ainda não possui Pix Automático criado.');const secrets=await bankSecrets(),r=await bankCall({action:'efi-pix-auto-refresh',idRec:record.idRec,efi:secrets.efi});Object.assign(record,r);save(s);return clone(record)}
  async function saveMpWeb(data){const s=load(),old=await secureGet('mercadoPago'),sec={...old};if(String(data.accessToken||'').trim())sec.accessToken=String(data.accessToken).trim();await secureSet('mercadoPago',sec);s.banks.mercadoPago={...s.banks.mercadoPago,enabled:Boolean(data.enabled),environment:data.environment==='production'?'production':'sandbox',publicKey:String(data.publicKey??s.banks.mercadoPago.publicKey??'').trim(),accessTokenConfigured:Boolean(sec.accessToken)};save(s);return clone(s.banks.mercadoPago)}
  async function testMpWeb(){const s=load(),secrets=await bankSecrets();try{const r=await bankCall({action:'mp-test',mercadoPago:secrets.mercadoPago});s.banks.mercadoPago.lastTestAt=r.checkedAt||now();s.banks.mercadoPago.lastTestStatus='success';s.banks.mercadoPago.lastTestMessage=r.message||'Conectado.';save(s);return r}catch(e){s.banks.mercadoPago.lastTestAt=now();s.banks.mercadoPago.lastTestStatus='error';s.banks.mercadoPago.lastTestMessage=e.message;save(s);throw e}}
  function localApi(){
    return {
      app:{info:async()=>({version:'1.0.17 Web',platform:'web',currentUser:load().settings.current_user_name||'Administrador',databasePath:'Navegador / migração para PostgreSQL'})},
      dashboard:{get:async()=>{const s=load(),month=new Date().toISOString().slice(0,7);return {active_clients:s.clients.filter(c=>c.status==='Ativo').length,overdue_clients:s.clients.filter(c=>c.status==='Em atraso').length,blocked_clients:s.clients.filter(c=>c.status==='Bloqueado').length,monthly_revenue_cents:s.invoices.filter(i=>i.status==='Pago'&&String(i.paid_at||i.created_at).slice(0,7)===month).reduce((a,i)=>a+(Number(i.amount_cents)||0),0),pending_cents:s.invoices.filter(i=>i.status==='Pendente').reduce((a,i)=>a+(Number(i.amount_cents)||0),0),open_tickets:s.tickets.filter(t=>!['Concluído','Fechado'].includes(t.state)).length,recent_clients:s.clients.slice(-5).reverse().map(m),recent_payments:s.invoices.filter(i=>i.status==='Pago').slice(-5).reverse().map(inv)}}},
      clients:{list:async()=>load().clients.map(m).sort((a,b)=>a.name.localeCompare(b.name)),nextContract:async()=>contract(load()),save:async data=>{const s=load();let x=data.id?byId(s.clients,data.id):null;if(!String(data.name||x?.name||'').trim())throw Error('Informe o nome ou a razão social do cliente.');if(x)Object.assign(x,data,{id:x.id,updated_at:now()});else{x={id:id(s,'clients'),person_type:'Pessoa física',trade_name:'',document:'',rg_ie:'',birth_date:'',phone:'',whatsapp:'',email:'',cep:'',street:'',address_number:'',complement:'',neighborhood:'',city:'',state:'',address_reference:'',plan_id:null,contract_number:contract(s),auto_block:0,installation_date:'',due_day:10,status:'Ativo',connection_type:'PPPoE',pppoe_username:'',pppoe_password:'',mikrotik_profile:'default',ip:'',mac_address:'',notes:'',mikrotik_secret_id:'',mikrotik_status:'Não sincronizado',last_mikrotik_sync:'',router_id:null,device_ip:'',device_port:0,created_at:now(),...data};s.clients.push(x)}audit(s,data.id?'update':'create','client',x.id);save(s);return m(x)},setMikrotikState:async(i,d)=>{const s=load(),x=byId(s.clients,i);if(x)Object.assign(x,{mikrotik_secret_id:d.secretId||'',mikrotik_status:d.status||'Não sincronizado',last_mikrotik_sync:d.lastSync||now()});save(s);return m(x)},status:async i=>{const x=byId(load().clients,i);return {client:m(x),trust:{usedThisMonth:false,active:false},traffic:{current:{download_bytes:0,upload_bytes:0},history:[]},router:null,pppoe:null,device:null}},block:async i=>{const s=load(),x=byId(s.clients,i);if(!x)throw Error('Cliente não encontrado.');x.status='Bloqueado';x.mikrotik_status='Pendente no agente local';save(s);return m(x)},unblock:async i=>{const s=load(),x=byId(s.clients,i);if(!x)throw Error('Cliente não encontrado.');x.status='Ativo';x.mikrotik_status='Pendente no agente local';save(s);return m(x)},trustRelease:async(i,h=48)=>{const s=load(),x=byId(s.clients,i);if(!x)throw Error('Cliente não encontrado.');x.status='Ativo';x.trust_release_at=now();x.trust_release_until=new Date(Date.now()+Number(h||48)*3600000).toISOString();save(s);return m(x)},openRouter:async()=>({opened:false,message:'Na versão web, o acesso ao equipamento usa o agente local seguro.'}),delete:async i=>{const s=load();s.clients=s.clients.filter(x=>Number(x.id)!==Number(i));s.invoices=s.invoices.filter(x=>Number(x.client_id)!==Number(i));save(s);return {ok:true}}},
      address:{lookupCep:async cep=>{const digits=String(cep||'').replace(/\D/g,'');if(digits.length!==8)throw Error('Informe um CEP com 8 números.');const r=await fetch(`https://viacep.com.br/ws/${digits}/json/`);if(!r.ok)throw Error('Não foi possível consultar o CEP.');const d=await r.json();if(d.erro)throw Error('CEP não encontrado.');return {cep:d.cep||'',street:d.logradouro||'',neighborhood:d.bairro||'',city:d.localidade||'',state:d.uf||'',complement:d.complemento||''}}},
      plans:{list:async()=>{const s=load();return s.plans.map(p=>({...p,client_count:s.clients.filter(c=>Number(c.plan_id)===Number(p.id)).length})).sort((a,b)=>a.price_cents-b.price_cents)},save:async data=>{const s=load();let x=data.id?byId(s.plans,data.id):null;if(!String(data.name||x?.name||'').trim())throw Error('Informe o nome do plano.');if(x)Object.assign(x,data,{id:x.id});else{x={id:id(s,'plans'),name:'',speed:'',price_cents:0,download_mbps:0,upload_mbps:0,active:1,created_at:now(),...data};s.plans.push(x)}save(s);return {...x,client_count:s.clients.filter(c=>Number(c.plan_id)===Number(x.id)).length}},delete:async i=>{const s=load();if(s.clients.some(c=>Number(c.plan_id)===Number(i)))throw Error('Este plano está vinculado a clientes.');s.plans=s.plans.filter(x=>Number(x.id)!==Number(i));save(s);return {ok:true}}},
      invoices:{list:async()=>load().invoices.map(inv).sort((a,b)=>String(b.due_date).localeCompare(String(a.due_date))),save:saveInvoiceWeb,generateInstallments:installmentsWeb,markPaid:markPaidWeb,sync:syncInvoicesWeb,preview:previewInvoiceWeb,download:downloadInvoiceWeb,delete:deleteInvoiceWeb,deleteByClient:deleteClientInvoicesWeb},
      tickets:{list:async()=>{const s=load();return s.tickets.map(t=>({...t,client_name:byId(s.clients,t.client_id)?.name||'Sem cliente'})).sort((a,b)=>b.id-a.id)},save:async data=>{const s=load();let x=data.id?byId(s.tickets,data.id):null;if(!String(data.subject||x?.subject||'').trim())throw Error('Informe o assunto do chamado.');if(x)Object.assign(x,data,{id:x.id});else{x={id:id(s,'tickets'),client_id:null,subject:'',priority:'Normal',state:'Aguardando',created_at:now(),...data};s.tickets.push(x)}save(s);return {...x,client_name:byId(s.clients,x.client_id)?.name||'Sem cliente'}},updateState:async(i,state)=>{const s=load(),x=byId(s.tickets,i);if(x)x.state=state;save(s);return {...x,client_name:byId(s.clients,x.client_id)?.name||'Sem cliente'}}},
      stock:{list:async()=>load().stock.sort((a,b)=>a.name.localeCompare(b.name)),save:async data=>{const s=load();let x=data.id?byId(s.stock,data.id):null;if(!String(data.name||x?.name||'').trim())throw Error('Informe o item.');if(x)Object.assign(x,data,{id:x.id});else{x={id:id(s,'stock'),name:'',category:'',quantity:0,min_quantity:0,unit:'un',unit_price_cents:0,...data};s.stock.push(x)}save(s);return x}},
      settings:{get:async()=>clone(load().settings),save:async values=>{const s=load();s.settings={...s.settings,...values};save(s);return clone(s.settings)}},
      banks:{get:bankGet,saveDefault:saveDefaultBank,saveEfi:saveEfiWeb,selectEfiCertificate:selectEfiCertificateWeb,removeEfiCertificate:removeEfiCertificateWeb,testEfi:testEfiWeb,configureEfiWebhooks:webhooksEfiWeb,createEfiPixAutomatic:createPixAutoWeb,refreshEfiPixAutomatic:refreshPixAutoWeb,saveMercadoPago:saveMpWeb,testMercadoPago:testMpWeb},
      routers:{list:async()=>load().routers.map(r=>({...r,has_password:Boolean(r.has_password),client_count:load().clients.filter(c=>Number(c.router_id)===Number(r.id)).length})),save:async data=>{const s=load();let x=data.id?byId(s.routers,data.id):null;if(!String(data.name||x?.name||'').trim()||!String(data.host||x?.host||'').trim())throw Error('Informe nome e IP do MikroTik.');if(x)Object.assign(x,data,{id:x.id,has_password:Boolean(data.password||x.has_password),updated_at:now()});else{x={id:id(s,'routers'),name:'',connection_method:'api',host:'',port:8728,username:'',allow_self_signed:0,enabled:1,has_password:Boolean(data.password),created_at:now(),updated_at:now(),...data};s.routers.push(x)}delete x.password;save(s);return x},delete:async i=>{const s=load();if(s.clients.some(c=>Number(c.router_id)===Number(i)))throw Error('Este MikroTik está vinculado a clientes.');s.routers=s.routers.filter(x=>Number(x.id)!==Number(i));save(s);return {ok:true}}},
      backup:{create:async()=>{download(`provedor-plus-backup-${new Date().toISOString().slice(0,10)}.json`,JSON.stringify(load(),null,2),'application/json');return {saved:true}},restore:async()=>{const input=document.createElement('input');input.type='file';input.accept='.json,application/json';return new Promise((resolve,reject)=>{input.onchange=()=>{const f=input.files?.[0];if(!f)return resolve({canceled:true});const rd=new FileReader();rd.onload=()=>{try{const d=JSON.parse(String(rd.result));save({...empty(),...d});location.reload();resolve({restored:true})}catch(e){reject(Error('Backup inválido.'))}};rd.readAsText(f)};input.click()})}},
      reports:{exportCsv:async kind=>{const s=load();let rows=kind==='clients'?s.clients.map(m):kind==='invoices'?s.invoices.map(inv):kind==='overdue'?s.invoices.filter(i=>i.status==='Pendente'&&String(i.due_date)<new Date().toISOString().slice(0,10)).map(inv):kind==='tickets'?await localApi().tickets.list():kind==='stock'?s.stock:s.plans;if(!rows.length)rows=[{Resultado:'Nenhum dado'}];const headers=Object.keys(rows[0]),csv='\uFEFF'+headers.join(';')+'\r\n'+rows.map(r=>headers.map(h=>`"${String(r[h]??'').replaceAll('"','""')}"`).join(';')).join('\r\n');download(`relatorio-${kind}.csv`,csv,'text/csv;charset=utf-8');return {saved:true}},exportOverduePdf:async()=>{const rows=load().invoices.filter(i=>i.status==='Pendente'&&String(i.due_date)<new Date().toISOString().slice(0,10));const w=open('','_blank');w.document.write('<meta charset=utf-8><title>Inadimplentes</title><h1>Relatório de inadimplentes</h1>'+rows.map(i=>{const x=inv(i);return `<p><b>${x.client_name}</b> · ${x.due_date} · R$ ${(x.amount_cents/100).toFixed(2)}</p>`}).join('')+'<script>print()<\/script>');w.document.close();return {opened:true}}},
      mikrotik:{sync:async()=>({queued:true,message:'Sincronização enviada ao agente local.'}),profiles:async()=>[],remoteAccess:async()=>({mode:'agent',message:'Acesso remoto disponível quando o agente local estiver conectado.'}),savePppoe:async()=>({queued:true,message:'Alteração PPPoE enviada ao agente local.'}),deletePppoe:async()=>({queued:true,message:'Exclusão PPPoE enviada ao agente local.'})},
      vpn:{status:async()=>({installed:false,web:true}),activate:async()=>({queued:false,message:'WireGuard é ativado pelo agente local no computador da rede.'}),remove:async()=>({queued:false}),save:async()=>({saved:false}),openWireGuard:async()=>{open('https://www.wireguard.com/install/','_blank');return {opened:true}}}
    };
  }
  const api=localApi();
  window.provedor=api;
})();
