import billingWorker,{runBillingCron} from './billing-cron.js';
import {neon} from '@neondatabase/serverless';
import {resolveRouterForService} from './worker-native-api.js';
import {handleMikrotikProxy} from './worker-mikrotik-native.js';

const STATE_KEY='web_state_v1017';
const TRUST_PATH='/api/customer-trust-release';
const PORTAL_ORIGINS=new Set(['https://cliente.fibramais.workers.dev','https://client.fibramais.workers.dev']);
const utf8=new TextEncoder();
const text=value=>String(value??'').trim();
const normalize=value=>text(value).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');
const bool=value=>value===true||['true','1','sim','yes','on'].includes(normalize(value));

function parseStateValue(value){if(value&&typeof value==='object'&&!Array.isArray(value))return value;if(typeof value==='string')try{const parsed=JSON.parse(value);return parsed&&typeof parsed==='object'&&!Array.isArray(parsed)?parsed:{}}catch{}return {}}
async function loadState(sql){const rows=await sql`SELECT value FROM pp_settings WHERE key=${STATE_KEY} LIMIT 1`;return parseStateValue(rows?.[0]?.value)}
async function saveState(sql,state){const at=new Date().toISOString(),raw=JSON.stringify(state||{});await sql`INSERT INTO pp_settings (key,value,updated_at) VALUES (${STATE_KEY},${raw}::jsonb,${at}) ON CONFLICT (key) DO UPDATE SET value=EXCLUDED.value,updated_at=EXCLUDED.updated_at`;return at}
function localClient(state,id){return (Array.isArray(state?.clients)?state.clients:[]).find(row=>Number(row?.id)===Number(id))||null}
function localClientIndex(state,id){return (Array.isArray(state?.clients)?state.clients:[]).findIndex(row=>Number(row?.id)===Number(id))}
function monthKey(date=new Date()){const p=new Intl.DateTimeFormat('en-US',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit'}).formatToParts(date),m={};for(const x of p)m[x.type]=x.value;return `${m.year}-${m.month}`}
function dateKeyBrazil(date=new Date()){const p=new Intl.DateTimeFormat('en-US',{timeZone:'America/Sao_Paulo',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(date),m={};for(const x of p)m[x.type]=x.value;return `${m.year}-${m.month}-${m.day}`}
function nextMonthIso(){const [y,m]=monthKey().split('-').map(Number),d=new Date(Date.UTC(y,m,1,3));return d.toISOString()}
function portalDate(value){if(!value)return '';const d=new Date(value);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'short',timeZone:'America/Sao_Paulo'}).format(d)}
function invoiceOpen(row){const s=normalize(row?.status);return ['pendente','pending','vencida','vencido','overdue','open','aberta','aberto'].some(x=>s.includes(x))}
function overdueRows(state,clientId){const today=dateKeyBrazil();return (Array.isArray(state?.invoices)?state.invoices:[]).filter(row=>Number(row?.client_id)===Number(clientId)&&invoiceOpen(row)&&text(row?.due_date||row?.dueDate).slice(0,10)<today)}
function hasOverdue(state,clientId){return overdueRows(state,clientId).length>0}
function maxOverdueDays(state,clientId){const today=Date.parse(`${dateKeyBrazil()}T12:00:00Z`);return overdueRows(state,clientId).reduce((max,row)=>{const due=Date.parse(`${text(row?.due_date||row?.dueDate).slice(0,10)}T12:00:00Z`);return Number.isFinite(due)?Math.max(max,Math.max(0,Math.floor((today-due)/86400000))):max},0)}
function appendAccessHistory(client,action,detail,user='Área do Cliente'){const current=Array.isArray(client?.access_history)?client.access_history:[];return [{at:new Date().toISOString(),action,user,detail},...current].slice(0,30)}
function trustActive(client){const until=new Date(text(client?.trust_release_until));return text(client?.trust_release_until)&&!Number.isNaN(until.getTime())&&until.getTime()>Date.now()}

function b64urlBytes(value){let raw=text(value).replace(/-/g,'+').replace(/_/g,'/');while(raw.length%4)raw+='=';const bin=atob(raw),out=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)out[i]=bin.charCodeAt(i);return out}
async function portalKey(env){const secret=text(env.PORTAL_SESSION_SECRET)||text(env.DATABASE_URL);if(!secret)throw Object.assign(new Error('Sessão segura da Área do Cliente não configurada.'),{statusCode:503});return crypto.subtle.importKey('raw',utf8.encode(secret),{name:'HMAC',hash:'SHA-256'},false,['verify'])}
async function verifySession(token,env){const parts=text(token).split('.');if(parts.length!==2)throw Object.assign(new Error('Sessão do cliente inválida. Entre novamente.'),{statusCode:401});try{const key=await portalKey(env),ok=await crypto.subtle.verify('HMAC',key,b64urlBytes(parts[1]),utf8.encode(parts[0]));if(!ok)throw Error('assinatura');const p=JSON.parse(new TextDecoder().decode(b64urlBytes(parts[0]))),clientId=Number(p?.clientId)||0,exp=Number(p?.exp)||0;if(!clientId||exp<=Date.now())throw Error('expirada');return {clientId}}catch{throw Object.assign(new Error('Sessão do cliente expirada ou inválida. Entre novamente.'),{statusCode:401})}}
function corsFor(request){const origin=text(request.headers.get('origin'));if(!PORTAL_ORIGINS.has(origin))return {'Vary':'Origin'};return {'Access-Control-Allow-Origin':origin,'Access-Control-Allow-Methods':'POST, OPTIONS','Access-Control-Allow-Headers':'Content-Type','Access-Control-Max-Age':'86400','Vary':'Origin'}}
function json(data,status=200,headers={}){return new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store, max-age=0',...headers}})}

async function mikrotikAction(env,action,client){const router=await resolveRouterForService(env,client.router_id),request=new Request('https://painel.fibramais.workers.dev/api/mikrotik-proxy',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,router,data:client})}),response=await handleMikrotikProxy(request);let body={};try{body=await response.json()}catch{}if(!response.ok||!body.ok)throw Object.assign(new Error(text(body?.error)||`Falha no MikroTik (HTTP ${response.status}).`),{statusCode:409});return body.data||{}}
async function mikrotikTruth(env,client){const snapshot=await mikrotikAction(env,'router.sync',client),username=text(client?.pppoe_username||client?.pppoe_user),secret=(Array.isArray(snapshot?.pppSecrets)?snapshot.pppSecrets:[]).find(item=>text(item?.name)===username);if(!secret)throw Object.assign(new Error('Não foi possível confirmar o acesso PPPoE deste cliente no MikroTik.'),{statusCode:409});return {blocked:Boolean(secret.disabled),secretId:text(secret.id)}}
async function portalClient(sql,id){const rows=await sql`SELECT id,name,document,contract_number,plan,plan_id,due_day,status,email,phone,address,city,state,zip_code,router_id,connection_type,pppoe_username,ip,mikrotik_status,mikrotik_last_sync FROM pp_clients WHERE id=${Number(id)} LIMIT 1`;return rows?.[0]||null}
async function persistNetwork(sql,state,id,truth,{status,clearTrust=false,patch={}}={}){const i=localClientIndex(state,id),at=new Date().toISOString(),nextStatus=status||(truth.blocked?'Bloqueado':hasOverdue(state,id)?'Em atraso':'Ativo'),mikrotikStatus=truth.blocked?'Bloqueado no MikroTik':'Sincronizado';if(i>=0)state.clients[i]={...state.clients[i],status:nextStatus,mikrotik_secret_id:truth.secretId||state.clients[i]?.mikrotik_secret_id||'',mikrotik_status:mikrotikStatus,mikrotik_last_sync:at,last_mikrotik_sync:at,...(clearTrust?{trust_release_until:''}:{}),...patch};await sql`UPDATE pp_clients SET status=${nextStatus},mikrotik_secret_id=COALESCE(NULLIF(${truth.secretId},''),mikrotik_secret_id),mikrotik_status=${mikrotikStatus},mikrotik_last_sync=${at},updated_at=${at} WHERE id=${Number(id)}`;return nextStatus}

function trustInfo(remote,local,state){const merged={...(local||{}),...(remote||{})},untilRaw=text(local?.trust_release_until),active=trustActive(local),usedMonth=text(local?.trust_release_used_month)||text(local?.trust_release_at).slice(0,7),usedThisMonth=usedMonth===monthKey(),status=normalize(merged.status),nextAvailableAt=usedThisMonth?nextMonthIso():'';let eligible=false,reasonCode='eligible',message='Você pode liberar sua conexão por 48 horas. Este benefício pode ser usado somente uma vez por mês.';if(active){reasonCode='active';message=`Sua liberação em confiança já está ativa até ${portalDate(untilRaw)}.`}else if(usedThisMonth){reasonCode='used_month';message=`A liberação em confiança já foi utilizada neste mês. Ela ficará disponível novamente em ${portalDate(nextAvailableAt)}.`}else if(status!=='bloqueado'){reasonCode='not_blocked';message='Sua conexão não está bloqueada. A liberação em confiança só é necessária quando o acesso estiver bloqueado.'}else if(text(merged.connection_type).toUpperCase()!=='PPPOE'){reasonCode='not_pppoe';message='A liberação em confiança está disponível somente para clientes com conexão PPPoE.'}else if(!Number(merged.router_id)||!text(merged.pppoe_username||merged.pppoe_user)){reasonCode='missing_configuration';message='Não foi possível liberar porque o acesso PPPoE ainda não está vinculado corretamente ao MikroTik. Entre em contato com o suporte.'}else eligible=true;return {eligible,active,usedThisMonth,usedMonth,startedAt:text(local?.trust_release_at),until:untilRaw,nextAvailableAt,reasonCode,message,status:text(merged.status),hasOverdue:hasOverdue(state,merged.id)}}

async function handleTrust(request,env){const cors=corsFor(request);if(request.method==='OPTIONS')return new Response(null,{status:204,headers:cors});if(request.method!=='POST')return json({ok:false,error:'Método não permitido.'},405,cors);try{const origin=text(request.headers.get('origin'));if(!PORTAL_ORIGINS.has(origin))throw Object.assign(new Error('Origem não autorizada.'),{statusCode:403});if(!env.DATABASE_URL)throw Object.assign(new Error('Conexão com o Provedor Plus não configurada.'),{statusCode:503});let body={};try{body=await request.json()}catch{}const action=text(body?.action),session=await verifySession(body?.data?.session,env),sql=neon(env.DATABASE_URL),remote=await portalClient(sql,session.clientId);if(!remote)throw Object.assign(new Error('Cliente não encontrado.'),{statusCode:404});const state=await loadState(sql),local=localClient(state,remote.id),info=trustInfo(remote,local,state);if(action==='status')return json({ok:true,data:info},200,cors);if(action!=='release')throw Object.assign(new Error('Ação de liberação não permitida.'),{statusCode:400});if(!info.eligible)return json({ok:false,error:info.message,data:info},409,cors);const i=localClientIndex(state,remote.id);if(i<0)throw Object.assign(new Error('Cadastro do cliente ainda não está sincronizado com a Área do Cliente.'),{statusCode:409});const before=JSON.parse(JSON.stringify(state)),merged={...state.clients[i],...remote},at=new Date(),until=new Date(at.getTime()+48*3600000),releaseMonth=monthKey(at),nextStatus=hasOverdue(state,remote.id)?'Em atraso':'Ativo';let unblocked=false;try{await mikrotikAction(env,'client.unblock',merged);unblocked=true;const truth=await mikrotikTruth(env,merged);if(truth.blocked)throw Object.assign(new Error('O MikroTik não confirmou a liberação do acesso PPPoE.'),{statusCode:409});await persistNetwork(sql,state,remote.id,truth,{status:nextStatus,patch:{trust_release_used_month:releaseMonth,trust_release_at:at.toISOString(),trust_release_until:until.toISOString(),trust_release_source:'area-cliente',access_history:appendAccessHistory(state.clients[i],'Liberação em confiança','Liberação temporária de 48 horas solicitada pela Área do Cliente')}});await saveState(sql,state)}catch(error){if(unblocked)try{await mikrotikAction(env,'client.block',merged)}catch{}try{const truth=await mikrotikTruth(env,merged);await persistNetwork(sql,before,remote.id,truth,{clearTrust:true});await saveState(sql,before)}catch(reconcile){throw Object.assign(new Error(`${error instanceof Error?error.message:error} Não foi possível realinhar o estado com o MikroTik: ${reconcile instanceof Error?reconcile.message:reconcile}`),{statusCode:409})}throw error}const freshLocal=localClient(state,remote.id),fresh={...remote,status:nextStatus};return json({ok:true,data:trustInfo(fresh,freshLocal,state)},200,cors)}catch(error){return json({ok:false,error:error instanceof Error?error.message:String(error)},Number(error?.statusCode)||500,cors)}}

async function processExpiredTrust(env){if(!env.DATABASE_URL)return {checked:0,reblocked:0,releasedByPayment:0,failed:0};const sql=neon(env.DATABASE_URL),state=await loadState(sql),items=Array.isArray(state.clients)?state.clients:[],expired=items.filter(c=>{const d=new Date(text(c?.trust_release_until));return text(c?.trust_release_until)&&!Number.isNaN(d.getTime())&&d.getTime()<=Date.now()});let reblocked=0,releasedByPayment=0,failed=0,changed=false;for(const local of expired){const id=Number(local?.id)||0;if(!id)continue;try{const remote=await portalClient(sql,id),i=localClientIndex(state,id);if(!remote||i<0){failed++;continue}if(!hasOverdue(state,id)){const at=new Date().toISOString();state.clients[i]={...state.clients[i],status:'Ativo',trust_release_until:'',trust_release_completed_at:at,trust_release_completion_reason:'payment',access_history:appendAccessHistory(state.clients[i],'Fim da liberação em confiança','Prazo encerrado sem novo bloqueio porque não há fatura vencida')};await sql`UPDATE pp_clients SET status='Ativo',updated_at=${at} WHERE id=${id}`;releasedByPayment++;changed=true;continue}const merged={...state.clients[i],...remote};await mikrotikAction(env,'client.block',merged);const truth=await mikrotikTruth(env,merged);if(!truth.blocked)throw Error('O MikroTik não confirmou o re-bloqueio do acesso PPPoE.');await persistNetwork(sql,state,id,truth,{clearTrust:true,patch:{trust_release_reblocked_at:new Date().toISOString(),trust_release_completion_reason:'expired',access_history:appendAccessHistory(state.clients[i],'Fim da liberação em confiança','48 horas encerradas; acesso PPPoE bloqueado novamente')}});reblocked++;changed=true}catch(error){failed++;console.error(`Provedor Plus: falha ao encerrar confiança do cliente ${id}.`,error)}}if(changed)await saveState(sql,state);return {checked:expired.length,reblocked,releasedByPayment,failed}}

async function processAutomaticDelinquency(env){if(!env.DATABASE_URL)return {checked:0,blocked:0,released:0,failed:0};const sql=neon(env.DATABASE_URL),state=await loadState(sql),items=Array.isArray(state.clients)?state.clients:[];let checked=0,blocked=0,released=0,failed=0,changed=false;for(const local of items){const id=Number(local?.id)||0;if(!id||normalizedConnection(local)!=='pppoe'||!Number(local.router_id)||!text(local.pppoe_username||local.pppoe_user))continue;const auto=bool(local.auto_block),days=maxOverdueDays(state,id),limit=Math.max(1,Math.min(365,Number(local.block_after_days)||7)),systemBlocked=local.auto_blocked_by_system===true,manualBlocked=/bloque|suspens/.test(normalize(local.status))&&!systemBlocked;checked++;try{if(auto&&days>=limit&&!trustActive(local)&&!manualBlocked){if(systemBlocked&&normalize(local.status).includes('bloqueado'))continue;const remote=await portalClient(sql,id),merged={...local,...remote};await mikrotikAction(env,'client.block',merged);const truth=await mikrotikTruth(env,merged);if(!truth.blocked)throw Error('O MikroTik não confirmou o bloqueio automático.');const at=new Date().toISOString();await persistNetwork(sql,state,id,truth,{status:'Bloqueado',patch:{auto_blocked_by_system:true,auto_blocked_at:at,auto_block_reason:`Mensalidade vencida há ${days} dia(s)`,access_history:appendAccessHistory(local,'Bloqueio automático',`Atraso de ${days} dia(s); limite configurado: ${limit} dia(s)`,'Automação')}});blocked++;changed=true;continue}if(systemBlocked&&(!auto||!hasOverdue(state,id))&&!trustActive(local)){const remote=await portalClient(sql,id),merged={...local,...remote};await mikrotikAction(env,'client.unblock',merged);const truth=await mikrotikTruth(env,merged);if(truth.blocked)throw Error('O MikroTik não confirmou a liberação automática.');const at=new Date().toISOString(),reason=!auto?'Bloqueio automático desativado no cadastro':'Mensalidades regularizadas';await persistNetwork(sql,state,id,truth,{status:hasOverdue(state,id)?'Em atraso':'Ativo',patch:{auto_blocked_by_system:false,auto_released_at:at,auto_block_reason:'',access_history:appendAccessHistory(local,'Liberação automática',`${reason}; acesso liberado após confirmação no MikroTik`,'Automação')}});released++;changed=true}}catch(error){failed++;console.error(`Provedor Plus: falha na automação de inadimplência do cliente ${id}.`,error)}}if(changed)await saveState(sql,state);return {checked,blocked,released,failed}}
function normalizedConnection(client){return normalize(client?.connection_type||'PPPoE')}

export default {
 fetch(request,env,ctx){const url=new URL(request.url);if(url.pathname===TRUST_PATH)return handleTrust(request,env);return billingWorker.fetch(request,env,ctx)},
 scheduled(controller,env,ctx){const task=(async()=>{await processExpiredTrust(env);await processAutomaticDelinquency(env);if(controller?.cron==='5 6 * * *')await runBillingCron(env)})();ctx.waitUntil(task.catch(error=>console.error('Provedor Plus: falha na rotina agendada.',error)))}
};
